import React from 'react';
import Checkbox, { CheckboxProps } from '@mui/material/Checkbox';

const MuiCheckbox: React.FC<CheckboxProps> = (props) => <Checkbox {...props} />;
export default MuiCheckbox;
