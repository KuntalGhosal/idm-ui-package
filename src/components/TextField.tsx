import React from 'react';
import TextField, { TextFieldProps } from '@mui/material/TextField';

const MuiTextField: React.FC<TextFieldProps> = (props) => <TextField {...props} />;
export default MuiTextField;
