import React from 'react';
import IconButton, { IconButtonProps } from '@mui/material/IconButton';

const MuiIconButton: React.FC<IconButtonProps> = (props) => <IconButton {...props} />;
export default MuiIconButton;
